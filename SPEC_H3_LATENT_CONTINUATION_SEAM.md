# SPEC — TJ_H3_LatentContinuation 이음매 아티팩트 (측정 자료 + 수정 제안)

대상: `nodes/video/h3_latent_continuation.py`
보고: `ComfyUI-TJ_NODE_STUDIO_ONE` v1.18.0 작업 중 실측

---

## 증상

One-Take로 이어붙인 클립은 **2번째부터 프레임 39~42에서 색이 깨집니다.** 9클립 런에서
연속성이 걸린 8개 전부 같은 자리, 같은 규모로 발생했습니다.

깨지는 양상은 **채널별 붕괴 후 완전 복구**입니다:

```
프레임 38: R105 G108 B118   ← 보존 구간 끝, 정상
프레임 39: R 39 G119 B119   ← R 채널 붕괴
프레임 40: R 58 G116 B101
프레임 41: R112 G 40 B 93   ← G 채널 붕괴
프레임 42: R102 G118 B135   ← 회복 시작
프레임 43: R106 G111 B119   ← 정상
```

이음매 전후 평균이 거의 같습니다(클립2 기준 +5.0 / +2.1 / −2.3). **연속성 자체는 제대로
작동하고 있고**, 경계 3~4프레임만 망가집니다.

---

## 원인 (코드)

`overlap_frames=39` → `video_latent_t(39) = 12` latent 프레임이 보존됩니다. 영상 프레임으로는
0~38이 보존, 39부터 새로 생성입니다. **깨지는 위치와 정확히 일치합니다.**

현재 구현은 **하드 스플라이스**입니다 — 페더링이 없습니다:

```python
new_video = tgt_video.clone()
new_video[:, :, :k_v] = prev_video[:, :, -k_v:]   # 잘라 붙이기
video_mask = torch.ones_like(new_video[:, :1], dtype=torch.float32)
video_mask[:, :, :k_v] = 0.0                       # 그대로 보존
```

경계에서 latent가 불연속이고, VAE의 시간축 커널이 그 지점을 걸치면서 몇 프레임을 망가뜨리는
것으로 보입니다. (채널별로 극단값이 나오는 건 latent가 분포를 벗어났을 때의 전형적 양상입니다.)

---

## 가속기는 원인이 아니라 증폭 요인 — A/B 대조 결과

동일 조건(같은 시드 37, 같은 프롬프트, Reference 3장, 0.2MP, 8초)에서 **가속기만** 바꿔
측정했습니다. 프레임간 평균 RGB 변화량 기준:

| | baseline | 피크 | 배율 | 소요 |
|---|---|---|---|---|
| Spectrum + FirstBlockCache | 0.96 | **183.9** @f38 | 191× | 3.8분 |
| **가속기 전부 OFF** | 1.19 | **59.4** @f41 | **50×** | 7.8분 |

**가속기를 다 꺼도 이음매는 남습니다(50배).** 가속기는 약 3배 증폭할 뿐이고, 원인은
스플라이스 자체입니다. 참고로 가속기가 시간을 절반으로 줄여주므로, 이 문제 때문에 끄는 건
해법이 아닙니다.

해상도도 무관합니다 — 1.0MP에서 138, 0.2MP에서 184로 오히려 저해상도가 더 심했습니다.

---

## 하류에서 한 임시 조치 (STUDIO_ONE v1.18.0)

자동 스티치가 오버랩 39프레임(0~38)만 잘라내고 있어서 **깨진 39~42가 최종본에 그대로
남았습니다.** 갤러리 수동 스티치에 트림 프레임 입력 필드를 추가하고 기본값을 43(오버랩 +
가드 4)으로 뒀습니다. 자동 스티치는 39 유지입니다.

**이건 증상을 잘라내는 것이지 고치는 게 아닙니다.** 매 이음매마다 0.167초를 버리고 있고,
개별 클립 파일에는 여전히 아티팩트가 남습니다.

---

## 수정 제안

경계에서 급격히 끊지 말고 **몇 latent 프레임에 걸쳐 블렌딩**하는 방향입니다. 다만 아래는
검증된 구현이 아니라 방향 제안입니다 — 실제로 어느 방식이 H3에서 가장 깨끗한지는 실측이
필요합니다.

**A. mask 페더링 (변경 최소)**
`noise_mask`를 0/1 계단 대신 경계에서 완만하게 올립니다:

```python
video_mask[:, :, :k_v] = 0.0
feather = min(feather_frames, k_v)          # latent 프레임 단위
if feather > 0:
    ramp = torch.linspace(0, 1, feather + 2)[1:-1]
    video_mask[:, :, k_v - feather:k_v] = ramp.view(1, 1, -1, 1, 1)
```
보존 구간 끝자락을 부분적으로 재생성하게 해서, 샘플러가 경계를 스스로 메우게 합니다.

**B. latent 크로스페이드**
복사할 때 경계 몇 프레임을 두 클립의 가중 평균으로:
```python
new_video[:, :, :k_v] = prev_video[:, :, -k_v:]
# 경계 n프레임은 prev/tgt를 선형 보간
```
다만 `tgt_video`는 빈 latent라 의미 있는 값이 아닐 수 있어, A보다 효과가 불확실합니다.

**C. 노드에서 가드 프레임을 명시적으로 알려주기**
근본 해결과 별개로, 아티팩트 폭을 노드가 report에 담아주면 하류(스티치)가 정확히 그만큼만
잘라낼 수 있습니다. 지금은 STUDIO_ONE이 43을 경험값으로 들고 있습니다.

---

## 재현 자료

```
C:\AI\output\one_minimax_h3\
  MMH3_clip002_00032_.mp4   Spectrum+FBC ON,  seed 37, 0.2MP  → 피크 183.9 @f38
  MMH3_clip002_00033_.mp4   가속기 OFF,       seed 37, 0.2MP  → 피크  59.4 @f41
  MMH3_clip002_00031_.mp4   Spectrum,         seed 37, 1.0MP  → 피크 138.0 @f40
  metadata/*.json           각 클립의 생성 설정 (v1.16.0+ 는 파이프라인 축까지 기록)
```

측정 방법: ffmpeg로 rgb24 원시 프레임을 뽑아 프레임별 평균 RGB를 구하고, 인접 프레임 간
채널합 절대차를 봤습니다. 정상 구간(46~58프레임) 중앙값을 baseline으로 잡았습니다.
